import styled from "styled-components";

const StyledHeader = styled.header`
  color: green;
`;

const Home = () => {
  return (
    <div>
      <header>Hello</header>
    </div>
  );
};

export default Home;
